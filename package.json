{
  "name": "borrar-posts",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "csv": "^2.0.0",
    "express": "^4.16.2",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm server-compiled.js && npx babel server.js --out-file server-compiled.js --presets=es2015 && forever start server-compiled.js"
  },
  "author": "",
  "license": "ISC"
}
